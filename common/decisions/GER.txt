#Category only allowed for GER

GER_berlinprojekt = {
	GER_berlinprojekt_mission = {

		priority = 10

		available = {
			hidden_trigger = { always = no }
		}

		cancel_if_not_visible = yes

		days_mission_timeout = 500
		is_good = yes
		fire_only_once = yes

		highlight_states = {
			highlight_states_trigger = { state = 835 }
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout GER_berlinprojekt_mission"
			835 = { #Berlin
				remove_dynamic_modifier = { modifier = GER_berlinprojekt scope = GER }
			}
			country_event = GER.3
		}
	}
	GER_invest_in_berlinprojeckt = {

		priority = 9

		available = {
			hidden_trigger = { tag = GER }
		}

		cost = 50
		days_re_enable = 50

		cancel_if_not_visible = yes

		complete_effect = {
			add_days_mission_timeout  = {
				mission = GER_berlinprojekt_mission
				days = 30
			}
		}
	}
}